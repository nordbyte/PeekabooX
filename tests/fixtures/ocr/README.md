# OCR Fixtures

`ocr_sample.png` is a deterministic high-contrast image used by
`examples/cli/ocr-smoke.sh` to exercise file OCR, region OCR, JSON output,
confidence metadata, and word-level OCR output without a live desktop session.
